# @kumiki/machine-number-field

Pure-TypeScript finite state machine for the NumberField (spin button)
component — nullable numeric value, clamp + snap + page-step semantics.

See [APG Spin Button pattern](https://www.w3.org/WAI/ARIA/apg/patterns/spinbutton/).
