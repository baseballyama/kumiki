/**
 * `@kumiki/atelier/dialog` Vanilla CSS variant — barrel.
 *
 * Same compound surface as `@kumiki/components/dialog` (Root, Trigger,
 * Overlay, Content, Title, Description, Close), each with `kumiki-dialog-*`
 * classes and CSS custom properties for theming.
 */
import Root from './Root.svelte';
import Trigger from './Trigger.svelte';
import Overlay from './Overlay.svelte';
import Content from './Content.svelte';
import Title from './Title.svelte';
import Description from './Description.svelte';
import Close from './Close.svelte';

export { Root, Trigger, Overlay, Content, Title, Description, Close };
